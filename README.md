# coding-style

Personal Codex skill for applying Java coding style preferences.

The skill lives in `java-coding-style/` and can be installed or symlinked into
`${CODEX_HOME:-$HOME/.codex}/skills` when you want Codex to auto-discover it.

